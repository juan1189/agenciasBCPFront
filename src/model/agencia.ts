export class Agencia {
  constructor(public agencia: string, public distrito: string, public provincia: string,
              public departamento: string, public direccion: string, public latitud: number, public longitud: number) {

  }
}
